

export const resetForm=(setTitle,setDescription,setProductImage,setShowModal,setChoosingFile)=>{
    setTitle("");
    setDescription("");
    setProductImage([]);
    setShowModal(false);
    setChoosingFile(false);
}